# ===== HSSE requirements (Colab-friendly) =====
# NOTE: For Google Colab install in a fresh runtime, then restart runtime once.

# Core DL stack (Colab usually already has torch; keep unpinned to avoid CUDA wheel mismatch)
torch>=2.2,<2.8
transformers==4.41.2
datasets==2.19.1
sentencepiece==0.2.0
tokenizers==0.19.1
accelerate==0.31.0

# Machine Learning
scikit-learn==1.5.2
lightgbm==4.4.0
xgboost==2.1.2
catboost==1.2.5

# Data processing
pandas==2.2.2
numpy>=1.26,<2.2
nltk==3.8.1
openpyxl==3.1.2

# Visualization
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.24.1

# Utilities
tqdm==4.66.5
joblib==1.4.2
colorama==0.4.6

# Text transformations / metrics
sacrebleu==2.4.3

# Compatibility pin: keep modern httpx for Colab ecosystem (openai/gradio)
httpx>=0.24.1,<1

# Optional web UI
flask==3.0.3
